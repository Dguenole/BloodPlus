/**
 * Package SERVICE : contient la logique métier
 * 
 * 💡 EXPLICATION :
 * Les services contiennent les règles de gestion de l'application
 * Par exemple :
 * - Un donneur ne peut pas donner plus d'une fois tous les 2 mois
 * - On ne peut pas distribuer plus de sang qu'on en a en stock
 * - Une alerte doit être créée quand le stock est bas
 * 
 * Les services font le lien entre l'interface (UI) et les DAO
 */
package service;
